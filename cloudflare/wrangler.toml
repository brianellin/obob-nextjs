name = "obob-crossword-realtime"
main = "src/index.ts"
compatibility_date = "2024-12-30"

# Durable Objects configuration
[durable_objects]
bindings = [
  { name = "CROSSWORD_ROOM", class_name = "CrosswordRoom" }
]

# Migrations for Durable Objects
[[migrations]]
tag = "v1"
new_classes = ["CrosswordRoom"]

# Local development settings
[dev]
port = 8787
local_protocol = "http"

[vars]
# For local dev, use localhost. Override in Cloudflare dashboard for production.
PUZZLE_API_URL = "http://localhost:3000"
