---
description: Run all tests and verify the build
---

Please run the following checks for the OBOB app:

1. Run `pnpm lint` to check for linting errors
2. Run `pnpm build` to verify the production build works
3. Report any errors or warnings found
4. If there are errors, suggest fixes
